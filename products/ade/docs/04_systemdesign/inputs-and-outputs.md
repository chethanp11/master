# ADE Inputs and Outputs

> **Document**: System Design — Inputs and Outputs  
> **Version**: 1.0.0

---

## 1. Input Payloads

### 1.1 ade_v1 Flow

**Required**:
```json
{
  "dataset": "branded_cards_transactions"
}
```

**Optional**:
```json
{
  "prompt": "Analyze Q4 spending trends",
  "intent": "...",
  "question": "...",
  "instructions": "..."
}
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `dataset` | string | Yes | Dataset name (CSV file name) |
| `prompt` | string | No | Analyst question (primary input) |
| `intent` | string | No | Alternate intent input |
| `question` | string | No | Alternate question input |
| `instructions` | string | No | Alternate instructions input |

---

### 1.2 visualization Flow

**Required**:
```json
{
  "dataset": "branded_cards_transactions"
}
```

**Optional**:
```json
{
  "prompt": "Show me the trends"
}
```

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `dataset` | string | Yes | Dataset name |
| `prompt` | string | No | Optional context for summaries |

---

## 2. Datasets

### 2.1 Dataset Location

CSV files are staged under the ADE input directory:
- `products/ade/staging/input/`
- `products/ade/data/` (built-in datasets)

### 2.2 Built-in Dataset

**branded_cards_transactions**

Location: `products/ade/data/branded_cards_transactions.csv`

Used for demos and testing.

### 2.3 Dataset Requirements

| Requirement | Description |
|-------------|-------------|
| Format | CSV with headers |
| Encoding | UTF-8 |
| Time column | Optional, auto-detected |
| Numeric columns | At least one for metrics |

---

## 3. User Inputs (HITL)

### 3.1 viz_preferences

**Form ID**: `viz_preferences`

```yaml
title: "Visualization preferences"
mode: "choice_input"
required:
  - "chart_type"
  - "metric_focus"
defaults:
  chart_type: "bar"
  metric_focus: "mean"  # or "anomalies" for visualization flow
  include_hypothesis_checks: true
  notes: ""
```

| Field | Type | Options | Description |
|-------|------|---------|-------------|
| `chart_type` | string | bar, line, area, scatter | Chart visualization type |
| `metric_focus` | string | mean, sum, median, growth_rate, anomalies | Metric aggregation focus |
| `include_hypothesis_checks` | boolean | true/false | Run hypothesis tests |
| `notes` | string | free text | Optional user notes |

### 3.2 clarify_intent (ade_v1 only)

**Purpose**: Request clarification when dataset/metric/time window is missing.

**Condition**: Triggered when `intent_agent` sets `blocking_required: true`.

### 3.3 clarify_followup (ade_v1 only)

**Purpose**: Request additional clarification if needed.

**Condition**: Conditional on prior clarification state.

---

## 4. Plan Approval

### 4.1 PlanProposal Output

Generated by `plan_proposal_agent`:

```python
{
  "proposal_id": "...",
  "summary": "Execute analysis with bar chart, mean focus, hypothesis checks enabled",
  "estimated_steps": 12,
  "estimated_cost": "LOW",
  "requires_approval": true
}
```

### 4.2 User Actions

| Action | Result |
|--------|--------|
| **Approve** | Proceed to step execution |
| **Reject** | Trigger `planning_agent` for replan |

---

## 5. Output Artifacts

### 5.1 Primary Outputs

| Output | Format | Description |
|--------|--------|-------------|
| `business_report.html` | HTML | Primary stakeholder report |
| `decision_packet.html` | HTML | Supporting decision summary |

### 5.2 Optional Outputs (via export_pdf)

| Output | Format | Description |
|--------|--------|-------------|
| `ade.pdf` | PDF | Exported PDF report |
| `ade.html` | HTML | Exported HTML |
| `ade_stub.json` | JSON | Stub data for testing |

### 5.3 Output Location

```
products/ade/staging/output/
├── business_report.html
├── decision_packet.html
└── (optional exports)
```

---

## 6. Artifact Flow

### 6.1 Step-to-Step Data Flow

```
Payload
  │
  ├─▶ data_reader ──▶ columns, rows, series, data, x_field, y_field
  │
  ├─▶ viz_preferences ──▶ chart_type, metric_focus, include_hypothesis_checks
  │
  ├─▶ compute_business_metrics ──▶ totals, movers, anomalies, evidence_items
  │
  ├─▶ sufficiency_evaluator ──▶ confidence_level, downgrade_reasons
  │
  ├─▶ detect_anomalies ──▶ anomalies, evidence_items
  │
  ├─▶ hypothesis_test_* ──▶ status, reasoning, evidence_items
  │
  ├─▶ build_chart_spec ──▶ chart_spec
  │
  ├─▶ assemble_decision_packet ──▶ decision_packet
  │
  ├─▶ assemble_business_report ──▶ report
  │
  └─▶ render_*_html ──▶ HTML files
```

### 6.2 Template Reference Syntax

```yaml
# Tool output
"{{artifacts.tool.<tool_name>.output.<field>}}"

# User input
"{{artifacts.user_input.<form_id>.values.<field>}}"

# Agent output
"{{artifacts.agent.<agent_name>.output.<field>}}"

# Payload
"{{payload.<field>}}"
```

---

## 7. Evidence References

### 7.1 Evidence Structure

```python
evidence_refs: [
  {
    "dataset_id": "branded_cards_transactions",
    "columns": ["date", "amount", "category"]
  }
]
```

### 7.2 Trace References

```python
trace_refs: [
  {"step_id": "read"},
  {"step_id": "compute_business_metrics"},
  {"step_id": "compute_anomalies"},
  {
    "user_inputs": {
      "chart_type": "bar",
      "metric_focus": "mean"
    }
  }
]
```

---

## 8. BusinessReport Structure

### 8.1 Output Schema

```json
{
  "title": "Business Report",
  "generated_at_iso": "2026-01-12T10:00:00Z",
  "dataset_id": "branded_cards_transactions",
  "row_count": 1000,
  "period_labels": ["Q1", "Q2", "Q3", "Q4"],
  "series_count": 5,
  "executive_summary": [
    "Total spending increased 15% YoY",
    "Travel category shows highest growth"
  ],
  "key_findings": [...],
  "visuals": [...],
  "anomalies": [...],
  "recommendations": [...],
  "appendix": {...}
}
```

---

## 9. DecisionPacket Structure

### 9.1 Output Schema

```json
{
  "question": "What drove Q4 spending changes?",
  "decision_summary": "Decision assembled from sufficiency evaluation...",
  "confidence_level": "high",
  "assumptions": [
    "Inputs reflect the provided dataset",
    "No external data sources are used"
  ],
  "limitations": [
    "Hypothesis checks are heuristic",
    "Evidence tables are limited to uploaded rows"
  ],
  "sections": [
    {
      "section_id": "sufficiency",
      "title": "Data sufficiency",
      "narrative": "Confidence: high...",
      "claim_strength": "high",
      "visuals": [...],
      "evidence_refs": [...]
    },
    {
      "section_id": "hypotheses",
      "title": "Hypothesis checks",
      "narrative": "Data outage: rejected...",
      "claim_strength": "medium",
      "rejected_alternatives": [...]
    }
  ],
  "trace_refs": [...]
}
```

---

## Cross-References

- [architecture.md](architecture.md) — Component overview
- [flows.md](flows.md) — Flow step definitions
- [schemas.md](schemas.md) — Data schema details
