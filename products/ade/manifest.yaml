# Analytical Decision Engine manifest – v1
name: "ade"
display_name: "Analytical Decision Engine"
description: "Produces audit-ready analytical decisions with evidence, confidence, and traceability. Visuals are supporting artifacts, not the primary output."
version: "0.1.0"

default_flow: "visualization"

exposed_api:
  enabled: true
  allowed_flows:
    - "visualization"
    - "ade_v1"

ui_enabled: true
ui:
  enabled: true
  nav_label: "Analytical Decision Engine"
  panels:
    - id: "runner"
      title: "Run a Flow"
    - id: "runs"
      title: "Run History"
    - id: "approvals"
      title: "Approvals Queue"

flows:
  - "visualization"
  - "ade_v1"
